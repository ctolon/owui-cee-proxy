apiVersion: v1
kind: Service
metadata:
  name: owui-cee-proxy
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: owui-cee-proxy
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
